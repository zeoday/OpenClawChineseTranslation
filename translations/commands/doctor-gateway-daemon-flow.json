{
  "file": "src/commands/doctor-gateway-daemon-flow.ts",
  "description": "网关守护进程流程",
  "replacements": {
    "note(\"Gateway not running.\", \"Gateway\")": "note(\"网关未运行。\", \"网关\")",
    "note(params.gatewayDetailsMessage, \"Gateway connection\")": "note(params.gatewayDetailsMessage, \"网关连接\")",
    "note(\"LaunchAgent is listed but not loaded in launchd.\", `${params.title} LaunchAgent`)": "note(\"LaunchAgent 已列出但未在 launchd 中加载。\", `${params.title} LaunchAgent`)",
    "note(`${params.title} LaunchAgent repaired.`, `${params.title} LaunchAgent`)": "note(`${params.title} LaunchAgent 已修复。`, `${params.title} LaunchAgent`)",
    "note(formatPortDiagnostics(diagnostics).join(\"\\n\"), \"Gateway port\")": "note(formatPortDiagnostics(diagnostics).join(\"\\n\"), \"网关端口\")",
    "note(`Last gateway error: ${lastError}`, \"Gateway\")": "note(`最近网关错误: ${lastError}`, \"网关\")",
    "note(renderSystemdUnavailableHints({ wsl }).join(\"\\n\"), \"Gateway\")": "note(renderSystemdUnavailableHints({ wsl }).join(\"\\n\"), \"网关\")",
    "note(\"Gateway service not installed.\", \"Gateway\")": "note(\"网关服务未安装。\", \"网关\")",
    "message: \"Install gateway service now?\"": "message: \"现在安装网关服务？\"",
    "message: \"Gateway service runtime\"": "message: \"网关服务运行时\"",
    "message: \"Restart gateway service now?\"": "message: \"现在重启网关服务？\"",
    "note(`Gateway service install failed: ${String(err)}\`, \"Gateway\");": "note(`网关服务安装失败：${String(err)}\`，\"网关\");"
  }
}
