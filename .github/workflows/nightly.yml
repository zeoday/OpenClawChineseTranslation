# ============================================================
# OpenClaw 汉化发行版 - 最新版（Nightly）自动构建工作流
# 武汉晴辰天下网络科技有限公司 | https://qingchencloud.com/
# ============================================================
#
# 触发方式：
#   - 每小时42分自动运行
#   - 手动触发
#
# 版本说明：
#   - 最新版 (@nightly)：本工作流发布，每小时自动构建，追踪上游最新代码
#   - 稳定版 (@latest)：sync-and-release.yml 发布，手动打 tag 触发
#
# 用户安装：
#   npm install -g @qingchencloud/openclaw-zh@nightly
# ============================================================

name: 最新版构建 (Nightly)

on:
  schedule:
    # 每小时42分构建（错峰避免冲突）
    - cron: '42 * * * *'
  workflow_dispatch:
    inputs:
      force_build:
        description: '强制构建（跳过版本检查）'
        required: false
        default: 'false'
        type: boolean

env:
  UPSTREAM_REPO: openclaw/openclaw
  NODE_VERSION: '22'

permissions:
  contents: write
  packages: write

jobs:
  # ==================== Nightly 构建 ====================
  nightly-build:
    runs-on: ubuntu-latest
    
    steps:
      - name: 检出代码
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 设置 Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          registry-url: 'https://registry.npmjs.org'

      - name: 安装 pnpm
        uses: pnpm/action-setup@v4
        with:
          version: latest

      - name: 获取上游版本信息
        id: upstream
        run: |
          UPSTREAM_VERSION=$(curl -s https://registry.npmjs.org/openclaw/latest | jq -r '.version')
          echo "version=$UPSTREAM_VERSION" >> $GITHUB_OUTPUT
          echo "📦 上游版本: $UPSTREAM_VERSION"

      - name: 克隆上游代码
        run: |
          git clone --depth 1 https://github.com/${{ env.UPSTREAM_REPO }}.git openclaw
          cd openclaw
          COMMIT_SHA=$(git rev-parse --short HEAD)
          echo "commit_sha=$COMMIT_SHA" >> $GITHUB_OUTPUT
          echo "✅ 上游代码已克隆 (commit: $COMMIT_SHA)"

      - name: 应用汉化补丁
        id: apply
        run: |
          # 运行汉化并捕获输出
          OUTPUT=$(node cli/index.mjs apply --verbose 2>&1) || true
          echo "$OUTPUT"
          
          # 提取统计信息（使用 sed 替代 grep -P，兼容性更好）
          APPLIED=$(echo "$OUTPUT" | grep "总计:" | sed 's/.*应用 \([0-9]*\).*/\1/' || echo "0")
          EXISTED=$(echo "$OUTPUT" | grep "总计:" | sed 's/.*已存在 \([0-9]*\).*/\1/' || echo "0")
          FAILED=$(echo "$OUTPUT" | grep "总计:" | sed 's/.*未找到 \([0-9]*\).*/\1/' || echo "0")
          FILES_COUNT=$(echo "$OUTPUT" | grep -c "替换:" || echo "0")
          
          # 如果提取失败，设置默认值
          [ -z "$APPLIED" ] && APPLIED="0"
          [ -z "$EXISTED" ] && EXISTED="0"
          [ -z "$FAILED" ] && FAILED="0"
          
          echo "applied=$APPLIED" >> $GITHUB_OUTPUT
          echo "existed=$EXISTED" >> $GITHUB_OUTPUT
          echo "failed=$FAILED" >> $GITHUB_OUTPUT
          echo "files=$FILES_COUNT" >> $GITHUB_OUTPUT
          
          echo ""
          echo "📊 汉化统计："
          echo "   ✅ 应用规则: $APPLIED 条"
          echo "   ⏭️ 已存在: $EXISTED 条"
          echo "   ⚠️ 未匹配: $FAILED 条"
          echo "   📝 替换操作: $FILES_COUNT 次"

      - name: 验证汉化结果
        run: |
          node cli/index.mjs verify || true
          echo "✅ 汉化验证完成"

      - name: 构建 OpenClaw
        working-directory: openclaw
        run: |
          pnpm install
          pnpm run build
          echo "✅ 构建完成"

      - name: 修复构建产物中的图标
        run: |
          # 创建 Python 修复脚本
          cat > /tmp/fix_icon.py << 'SCRIPT_EOF'
          import re
          import sys
          import glob
          
          SVG_ICON = '<svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 16 16" style="display:block"><rect width="16" height="16" fill="none"/><g fill="#3a0a0d"><rect x="1" y="5" width="1" height="3"/><rect x="2" y="4" width="1" height="1"/><rect x="2" y="8" width="1" height="1"/><rect x="3" y="3" width="1" height="1"/><rect x="3" y="9" width="1" height="1"/><rect x="4" y="2" width="1" height="1"/><rect x="4" y="10" width="1" height="1"/><rect x="5" y="2" width="6" height="1"/><rect x="11" y="2" width="1" height="1"/><rect x="12" y="3" width="1" height="1"/><rect x="12" y="9" width="1" height="1"/><rect x="13" y="4" width="1" height="1"/><rect x="13" y="8" width="1" height="1"/><rect x="14" y="5" width="1" height="3"/><rect x="5" y="11" width="6" height="1"/><rect x="4" y="12" width="1" height="1"/><rect x="11" y="12" width="1" height="1"/><rect x="3" y="13" width="1" height="1"/><rect x="12" y="13" width="1" height="1"/><rect x="5" y="14" width="6" height="1"/></g><g fill="#ff4f40"><rect x="5" y="3" width="6" height="1"/><rect x="4" y="4" width="8" height="1"/><rect x="3" y="5" width="10" height="1"/><rect x="3" y="6" width="10" height="1"/><rect x="3" y="7" width="10" height="1"/><rect x="4" y="8" width="8" height="1"/><rect x="5" y="9" width="6" height="1"/><rect x="5" y="12" width="6" height="1"/><rect x="6" y="13" width="4" height="1"/></g><g fill="#ff775f"><rect x="1" y="6" width="2" height="1"/><rect x="2" y="5" width="1" height="1"/><rect x="2" y="7" width="1" height="1"/><rect x="13" y="6" width="2" height="1"/><rect x="13" y="5" width="1" height="1"/><rect x="13" y="7" width="1" height="1"/></g><g fill="#081016"><rect x="6" y="5" width="1" height="1"/><rect x="9" y="5" width="1" height="1"/></g><g fill="#f5fbff"><rect x="6" y="4" width="1" height="1"/><rect x="9" y="4" width="1" height="1"/></g></svg>'
          
          pattern = r'<img src="https://mintcdn\.com/[^"]*pixel-lobster\.svg[^"]*" alt="OpenClaw" />'
          
          for js_file in glob.glob("openclaw/dist/control-ui/assets/*.js"):
              with open(js_file, "r", encoding="utf-8") as f:
                  content = f.read()
              if "mintcdn.com" in content:
                  new_content = re.sub(pattern, SVG_ICON, content)
                  if content != new_content:
                      with open(js_file, "w", encoding="utf-8") as f:
                          f.write(new_content)
                      print(f"✅ 图标已替换: {js_file}")
                  else:
                      print(f"ℹ️ 未匹配到图标: {js_file}")
          SCRIPT_EOF
          
          python3 /tmp/fix_icon.py
          echo "✅ 图标修复完成"

      - name: 注入功能面板
        run: |
          python3 scripts/inject_panel.py
          echo "✅ 功能面板注入完成"

      - name: 准备 Nightly 版本
        id: prepare
        run: |
          UPSTREAM_VERSION="${{ steps.upstream.outputs.version }}"
          # 使用时间戳确保每小时版本唯一
          TIMESTAMP=$(date +%Y%m%d%H%M)
          NIGHTLY_VERSION="${UPSTREAM_VERSION}-nightly.${TIMESTAMP}"
          
          echo "nightly_version=$NIGHTLY_VERSION" >> $GITHUB_OUTPUT
          echo "build_date=$(date +%Y-%m-%d)" >> $GITHUB_OUTPUT
          echo "build_time=$(date +%H:%M)" >> $GITHUB_OUTPUT
          echo "📦 Nightly 版本: $NIGHTLY_VERSION"
          
          # 更新 package.json
          cd openclaw
          npm version $NIGHTLY_VERSION --no-git-tag-version --allow-same-version || true
          
          # 修改包信息
          jq '.name = "@qingchencloud/openclaw-zh"' package.json > tmp.json && mv tmp.json package.json
          jq '.description = "OpenClaw 汉化发行版 (Nightly) - 武汉晴辰天下网络科技有限公司"' package.json > tmp.json && mv tmp.json package.json
          
          # 使用中文 README
          cp ../README.md ./README.md

      - name: 发布到 npm (nightly 标签)
        working-directory: openclaw
        run: npm publish --access public --tag nightly
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        continue-on-error: true

      - name: 删除旧的 nightly 标签
        run: |
          git push origin :refs/tags/nightly || true
          echo "🗑️ 已删除旧的 nightly 标签"

      - name: 创建新的 nightly 标签
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git tag -fa nightly -m "Nightly build ${{ steps.prepare.outputs.build_date }} ${{ steps.prepare.outputs.build_time }}"
          git push origin nightly --force
          echo "🏷️ 已创建 nightly 标签"

      - name: 创建/更新 Nightly Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: nightly
          name: "🌙 Nightly Build - ${{ steps.prepare.outputs.build_date }} ${{ steps.prepare.outputs.build_time }}"
          prerelease: true
          body: |
            ## 🌙 OpenClaw 汉化版 - 最新版 (Nightly)
            
            > ⚠️ **警告**：此为每小时自动构建版本，追踪上游最新代码，可能包含未完全测试的更改。
            > 
            > 🔒 **需要稳定版？** 请安装 `@latest`：`npm install -g @qingchencloud/openclaw-zh@latest`
            
            **构建时间**: ${{ steps.prepare.outputs.build_date }} ${{ steps.prepare.outputs.build_time }} (UTC)
            **上游版本**: ${{ steps.upstream.outputs.version }}
            **Nightly 版本**: ${{ steps.prepare.outputs.nightly_version }}
            **发布类型**: 最新版 (Nightly) - 每小时自动构建
            
            ---
            
            ### 📊 汉化统计
            
            | 指标 | 数量 | 状态 |
            |------|------|------|
            | ✅ 应用规则 | **${{ steps.apply.outputs.applied }}** 条 | 成功 |
            | ⏭️ 已存在 | ${{ steps.apply.outputs.existed }} 条 | 跳过 |
            | ⚠️ 未匹配 | ${{ steps.apply.outputs.failed }} 条 | 需关注 |
            | 📝 替换操作 | ${{ steps.apply.outputs.files }} 次 | - |
            
            ---
            
            ### 📦 安装 Nightly 版本
            
            ```bash
            # 安装 nightly 版本
            npm install -g @qingchencloud/openclaw-zh@nightly
            
            # 切换回稳定版
            npm install -g @qingchencloud/openclaw-zh@latest
            ```
            
            ---
            
            ### 🔗 相关链接
            
            - 🔥 官网: https://openclaw.qt.cool/
            - 📖 稳定版: https://github.com/1186258278/OpenClawChineseTranslation/releases/latest
            - 📦 npm: https://www.npmjs.com/package/@qingchencloud/openclaw-zh
            
            ---
            
            **武汉晴辰天下网络科技有限公司** | https://qingchencloud.com/
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # ==================== Docker 镜像构建与推送 ====================
      - name: 准备 Docker 构建
        run: |
          # 复制 Dockerfile 和 .dockerignore 到构建目录
          cp Dockerfile openclaw/Dockerfile
          cp .dockerignore openclaw/.dockerignore 2>/dev/null || true
          
          # 验证构建产物存在
          echo "📦 检查构建产物..."
          ls -la openclaw/
          ls -la openclaw/dist/ || echo "⚠️ dist/ 目录不存在"
          ls -la openclaw/node_modules/ | head -20 || echo "⚠️ node_modules/ 目录不存在"
          
          # 确保 dist 目录存在
          if [ ! -d "openclaw/dist" ]; then
            echo "❌ 错误：dist/ 目录不存在，Docker 构建将失败"
            exit 1
          fi
          
          echo "✅ Docker 构建准备完成"

      - name: 设置 Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: 登录 GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: 准备 Docker 元数据
        id: docker_meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository_owner }}/openclaw-zh
          tags: |
            type=raw,value=nightly
            type=raw,value=${{ steps.prepare.outputs.nightly_version }}

      - name: 构建并推送 Docker 镜像
        uses: docker/build-push-action@v5
        with:
          context: ./openclaw
          push: true
          tags: ${{ steps.docker_meta.outputs.tags }}
          labels: ${{ steps.docker_meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          platforms: linux/amd64,linux/arm64
        continue-on-error: true

      - name: 设置 Docker 镜像为公开
        run: |
          echo "🔓 设置 Docker 镜像可见性为 public..."
          curl -X PATCH \
            -H "Authorization: Bearer ${{ secrets.GITHUB_TOKEN }}" \
            -H "Accept: application/vnd.github.v3+json" \
            https://api.github.com/user/packages/container/openclaw-zh/versions \
            -d '{"visibility":"public"}' || true
        continue-on-error: true

      - name: 构建摘要
        run: |
          echo "## 🌙 Nightly 构建完成" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| 项目 | 值 |" >> $GITHUB_STEP_SUMMARY
          echo "|------|-----|" >> $GITHUB_STEP_SUMMARY
          echo "| 构建时间 | ${{ steps.prepare.outputs.build_date }} ${{ steps.prepare.outputs.build_time }} |" >> $GITHUB_STEP_SUMMARY
          echo "| 上游版本 | ${{ steps.upstream.outputs.version }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Nightly 版本 | ${{ steps.prepare.outputs.nightly_version }} |" >> $GITHUB_STEP_SUMMARY
          echo "| 应用规则 | ${{ steps.apply.outputs.applied }} 条 |" >> $GITHUB_STEP_SUMMARY
          echo "| 未匹配规则 | ${{ steps.apply.outputs.failed }} 条 |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 📦 安装方式" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**npm**: \`npm install -g @qingchencloud/openclaw-zh@nightly\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Docker**: \`docker pull ghcr.io/${{ github.repository_owner }}/openclaw-zh:nightly\`" >> $GITHUB_STEP_SUMMARY
